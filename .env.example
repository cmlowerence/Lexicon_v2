# Public API origin used by the frontend in production.
# Keep this to the deployed backend host (no trailing slash required).
VITE_API_ORIGIN=https://qubitgyan-api.onrender.com

# Base path for lexicon endpoints.
# Can be set with or without leading/trailing slash; app normalizes it.
VITE_LEXICON_BASE=/api/v2/lexicon
